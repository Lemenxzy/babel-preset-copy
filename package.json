{
  "name": "babel-preset-copy",
  "version": "1.0.2",
  "description": "Babel preset to transform JSX into Copy.copyCreateElement() calls",
  "main": "index.js",
  "scripts": {
    "start": "node test/run.js test/test.copy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "babel-plugin-syntax-jsx": "^6.18.0",
    "babel-plugin-transform-react-jsx": "^6.24.1"
  },
  "devDependencies": {
    "babel-core": "^6.26.3"
  },
  "peerDependencies": {
    "babel-core": "^6.26.3"
  }
}
